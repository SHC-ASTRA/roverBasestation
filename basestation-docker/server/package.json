{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "tsc-test": "yarn tsc-build && yarn prod",
    "dev": "yarn build && node server.js",
    "prod": "node server.js",
    "react-build": "cd ../react-app && yarn build",
    "tsc-build": "tsc --build",
    "build": "yarn react-build && yarn tsc-build",
    "clean": "tsc --build --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "rclnodejs": "^0.23.1",
    "rclnodejs-cli": "^0.3.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "nodemon": "^3.0.1"
  }
}
